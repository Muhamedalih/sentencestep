# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# editor
.vscode/*
!.vscode/extensions.json
.idea

# Claude Code: local-only session/permission state, never shared config
.claude/settings.local.json
.claude/scheduled_tasks.lock

# One-off content-authoring scratch work (audit scripts, data dumps,
# book-content backups from past Library authoring passes) — never part of
# the app, referenced by at most a doc comment as an audit trail, and some
# of it holds copyrighted book text that must never end up in a commit.
scripts/scratch/
